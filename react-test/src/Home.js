import React from 'react'

export const Home = () => (
  <div>
    <h2>People</h2>
    <p>Foreach statment for each person with in DB</p>
    <p>then get person's ID and SQL query to DB for user infomation e.g. Address and etc</p>
    <p>replace input boxes with related info on person</p>
    <p>Make form <form> </form> to update changed info</p>
    <p>Reacts 'differet' to what im use to (ASP.Net Razor Pages), but I can export my skill to react</p>
    </div>
  //var rows = [];
  //Change to Persons.lengh or something
  //for (var i = 0; i < numrows; i++) {  
  //rows.push(<ObjectRow key={i} />);
  //}
  //return <tbody>{rows}</tbody>;
)
